{
  "private": true,
  "name": "xai-finance-worker",
  "version": "1.0.0",
  "description": "Cloudflare worker for XAI Finance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
} 